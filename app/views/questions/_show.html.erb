<div class = "question_show_partial" >
  <div class = "question_votes" >
    <% if logged_in? %>
      <%= render partial: "votes/vote_buttons", locals: {votable: question } %>
     <% end %>
  </div>
  <div class = "question_buttons" >
    <% if logged_in? %>
      <% if question.user_id == current_user.id || current_user.is_admin %>
        <%= button_to "Edit", "/questions/#{question.id}/edit", method: :get %>
        <%= button_to "Delete", question, {method: "delete", confirm: "Are you sure you want to delete this question?"} %>
      <% end %>
    <% end %>
  </div>

  <ul class = "question_content" >

    <%# the HTML here is a bit wonky.
      a UL should have LIs immediately under it, it goesn't make sense for a list to have multiple headings as children

    Also, prefer:
      link_to question.title, question class: "big-title-text"

    It's the HTML structure that's at issue here.
    %>

    <a href="/questions/<%=question.id%>"><h2><%= question.title %></h2></a>
    <h4><%= question.updated_at %></h4>
    <h4><%= link_to question.user.name, user_path(question.user) %></h4>
    </li>
    <li class = "question_body" >
      <%= question.body %>
    </li>
    <li class = "question_tag" >
      Tags: <%= question.display_tags %>
    </li>
  </ul>
</div>

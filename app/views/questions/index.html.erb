<div class='container'>
  <%= button_to "Unanswered Questions", {action: "unanswered_questions"}, method: "get" %>
  <% @questions.each do |question| %>
    <%= render partial: "questions/show", locals: {question: question} %>
  <% end %>
</div>

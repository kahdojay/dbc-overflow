<div class='container'>
  <% @questions.each do |q| %>
  <a href="/questions/<%= q.id %>" id="<%= q.id %>">
    <h2> <%= q.title %> <%= q.updated_at %> </h2>
    <p class='question_body'> <%= q.body %> </p>

  </a>

  <% end %>

</div>

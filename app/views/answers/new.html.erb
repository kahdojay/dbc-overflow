<p><h2>Add another answer!</h2></p>
  <%= form_for [@question, @answer_new] do |f| %>
    <p><%= f.text_field :body, placeholder: 'add an answer' %></p>
    <p><%= f.submit "Submit" %></p>
  <% end %>
